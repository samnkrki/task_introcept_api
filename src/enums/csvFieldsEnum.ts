export enum Gender {
    MALE = 'Male',
    FEMALE = 'Female',
    OTHERS = "Others"
}

export enum Nationality {
    NEPALI = "Nepali",
    OTHERS = "Others"
}

export enum PrefModeContact {
    EMAIL = 'Email',
    PHONE = 'Phone',
    NONE = 'None'
}