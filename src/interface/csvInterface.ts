export interface CsvHeader {
    title: string,
    id: string
}

export interface CsvBody {
    name: string,
    educationBackground: string,
    email: string,
    gender: string,
    dob: string,
    nationality: string,
    phone: string,
    prefModeContact: string,
    address: string,
}